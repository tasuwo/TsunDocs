# vim: syntax=ruby

git_url("git@github.com:tasuwo/certificates.git")
storage_mode("git")
readonly(true)

for_platform :ios do
  platform("ios")
  type("development")
  app_identifier([
    "net.tasuwo.tsundocs",
  ])

  # for_lane :release do
  #   type("appstore")
  #   readonly(true)
  #   app_identifier([
  #     "net.tasuwo.tsundocs",
  #   ])
  # end
end

for_platform :mac do
  platform("macos")
  type("development")
  app_identifier([
    "net.tasuwo.tsundocs",
  ])

  # for_lane :release do
  #   type("appstore")
  #   readonly(true)
  #   app_identifier([
  #     "net.tasuwo.tsundocs",
  #   ])
  # end
end

